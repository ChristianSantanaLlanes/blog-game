<div class="widget category-widget">
    <h3 class="widget-title">
        <svg
            class="category-icon"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
        >
            <rect x="3" y="3" width="7" height="7" rx="1"></rect>
            <rect x="14" y="3" width="7" height="7" rx="1"></rect>
            <rect x="3" y="14" width="7" height="7" rx="1"></rect>
            <rect x="14" y="14" width="7" height="7" rx="1"></rect>
        </svg>
        <span>Categorías</span>
        <span class="accent-line"></span>
    </h3>
    <div class="category-cloud">
        <a href="/news" class="category-tag news">
            <div class="tag-content">
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                >
                    <path
                        d="M4 22h16a2 2 0 002-2V4a2 2 0 00-2-2H8a2 2 0 00-2 2v16a2 2 0 01-2 2zm0 0a2 2 0 01-2-2v-9c0-1.1.9-2 2-2h2"
                    ></path>
                    <path d="M18 14h-8M18 18h-8M18 10h-8"></path>
                </svg>
                <span class="name">Noticias</span>
            </div>
            <span class="tag-arrow">→</span>
        </a>
        <a href="/guides" class="category-tag guides">
            <div class="tag-content">
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                >
                    <path d="M2 3h6a4 4 0 014 4v14a3 3 0 00-3-3H2z"></path>
                    <path d="M22 3h-6a4 4 0 00-4 4v14a3 3 0 013-3h7z"></path>
                </svg>
                <span class="name">Guías</span>
            </div>
            <span class="tag-arrow">→</span>
        </a>
        <a href="/tutorials" class="category-tag tutorials">
            <div class="tag-content">
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                >
                    <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
                <span class="name">Tutoriales</span>
            </div>
            <span class="tag-arrow">→</span>
        </a>
    </div>
</div>

<style>
    .widget {
        margin-bottom: 2.5rem;
    }

    .widget-title {
        font-size: 1.1rem;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        margin-bottom: 1.5rem;
        padding-bottom: 0.75rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .category-icon {
        width: 20px;
        height: 20px;
        stroke: var(--color-accent);
    }

    .accent-line {
        flex: 1;
        height: 2px;
        background: linear-gradient(90deg, var(--color-accent), transparent);
        margin-left: 10px;
        border-radius: 2px;
    }

    .category-cloud {
        display: grid;
        gap: 0.75rem;
    }

    .category-tag {
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.06);
        padding: 1rem 1.25rem;
        border-radius: 10px;
        text-decoration: none;
        display: flex;
        justify-content: space-between;
        align-items: center;
        transition: all var(--transition-normal);
        position: relative;
        overflow: hidden;
    }

    .category-tag::before {
        content: "";
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 3px;
        transition: width var(--transition-normal);
    }

    .category-tag.news::before {
        background: var(--color-primary);
    }
    .category-tag.guides::before {
        background: var(--color-secondary);
    }
    .category-tag.tutorials::before {
        background: var(--color-accent);
    }

    .category-tag:hover {
        transform: translateX(6px);
        background: rgba(255, 255, 255, 0.04);
        border-color: transparent;
    }

    .category-tag.news:hover {
        border-color: var(--color-primary);
        box-shadow: 0 0 20px var(--color-primary-glow);
    }
    .category-tag.guides:hover {
        border-color: var(--color-secondary);
        box-shadow: 0 0 20px var(--color-secondary-glow);
    }
    .category-tag.tutorials:hover {
        border-color: var(--color-accent);
        box-shadow: 0 0 20px var(--color-accent-glow);
    }

    .category-tag:hover::before {
        width: 5px;
    }

    .tag-content {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .tag-content svg {
        width: 20px;
        height: 20px;
        opacity: 0.6;
        transition: all var(--transition-fast);
    }

    .category-tag.news svg {
        stroke: var(--color-primary);
    }
    .category-tag.guides svg {
        stroke: var(--color-secondary);
    }
    .category-tag.tutorials svg {
        stroke: var(--color-accent);
    }

    .category-tag:hover svg {
        opacity: 1;
    }

    .name {
        color: rgba(255, 255, 255, 0.9);
        font-weight: 600;
        text-transform: uppercase;
        font-size: 0.95rem;
        letter-spacing: 0.5px;
    }

    .tag-arrow {
        color: rgba(255, 255, 255, 0.3);
        font-size: 1.2rem;
        transition: all var(--transition-fast);
    }

    .category-tag:hover .tag-arrow {
        color: var(--color-text);
        transform: translateX(3px);
    }
</style>
